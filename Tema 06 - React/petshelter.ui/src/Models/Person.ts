export class Person {
    id: number;
    name: string;
    contactNumber: string;

    constructor(id: number, name: string, contactNumber: string) {
        this.id = id;
        this.name = name;
        this.contactNumber = contactNumber;
    }
}